public class SyntaxErrors {
    public static void main(String[] args) {
        // Syntax Error Examples - these would normally not compile
        // This is a CORRECTED version for educational purposes
        
        int x = 5;  // Fixed: added semicolon
        System.out.println("x = " + x);
        
        boolean valid = true;  // Fixed: added proper syntax
        if (valid) {
            System.out.println("Valid code!");
        }
    }
}
